# requirements.txt
# Core dependencies for ChartLocker DL script
requests
beautifulsoup4
tqdm
tabulate
fuzzywuzzy
rich

# Optional dependencies
# For faster fuzzy matching:
python-Levenshtein
# For MediaFire API (premium) support:
mediafire
# For deflate64 ZIP extraction support:
zipfile-deflate64